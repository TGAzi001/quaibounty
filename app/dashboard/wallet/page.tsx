'use client'

import { useState } from 'react'
import { Check, Copy, WalletCards } from 'lucide-react'
import { DashboardShell, SectionHeader } from '@/components/dashboard-shell'

export default function WalletPage() { const [address, setAddress] = useState(''); const [saved, setSaved] = useState(false); const [error, setError] = useState(''); const save = () => { if (!/^0x[a-fA-F0-9]{6,64}$/.test(address.trim())) { setError('Enter a valid Quai-compatible wallet address.'); setSaved(false); return } setError(''); setSaved(true) }; return <DashboardShell title="Payout wallet"><SectionHeader eyebrow="Wallet" title="Payout wallet" description="Choose where QuaiBounty will send your bounty rewards."/><div className="mt-8 grid gap-5 lg:grid-cols-2"><section className="rounded-xl border border-[#bfe4cf] bg-green-wash p-6"><div className="flex items-start justify-between"><div><div className="text-xs font-semibold uppercase tracking-[.12em] text-[#08784d]">Connected</div><h2 className="mt-2 text-xl font-semibold text-[#071512]">BlipPay</h2></div><div className="grid size-10 place-items-center rounded-full bg-white text-[#08784d]"><WalletCards size={20}/></div></div><div className="mt-8 font-mono text-sm text-[#25372f]">0x7A3...91F <button aria-label="Copy wallet address" className="ml-2 text-[#08784d]"><Copy size={14}/></button></div><div className="mt-5 flex items-center gap-2 text-sm font-medium text-[#08784d]"><Check size={16}/>Ready for payouts · Quai network</div><button className="mt-6 rounded-lg border border-[#9bd9bd] bg-white px-4 py-2.5 text-sm font-semibold text-[#08784d]">Change wallet</button></section><section className="rounded-xl border border-border bg-card p-6"><h2 className="text-lg font-semibold">Set up your payout wallet</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Choose where you&apos;d like to receive your QUAI rewards.</p><button className="mt-6 flex w-full items-center justify-between rounded-lg border border-[#bfe4cf] bg-green-wash px-4 py-3 text-left"><span><span className="block text-sm font-semibold text-[#08784d]">Connect BlipPay</span><span className="mt-1 block text-xs text-muted-foreground">Recommended for Quai</span></span><span className="rounded-full bg-primary px-2 py-1 text-[10px] font-bold text-primary-foreground">Recommended</span></button><button className="mt-3 w-full rounded-lg border border-border px-4 py-3 text-sm font-semibold">Connect another wallet</button><div className="my-6 flex items-center gap-3 text-xs text-muted-foreground"><span className="h-px flex-1 bg-border"/>OR<span className="h-px flex-1 bg-border"/></div><label className="text-sm font-medium">Enter wallet address<input value={address} onChange={e => setAddress(e.target.value)} placeholder="0x..." className="mt-2 w-full rounded-lg border border-border bg-background px-3 py-2.5 font-mono text-sm outline-none focus:ring-2 focus:ring-ring"/></label>{error && <p className="mt-2 text-xs text-red-600">{error}</p>}{saved && <p className="mt-2 text-xs text-[#08784d]">Wallet address saved for this mock session.</p>}<button onClick={save} className="mt-4 rounded-lg bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground">Save wallet</button></section></div></DashboardShell> }
