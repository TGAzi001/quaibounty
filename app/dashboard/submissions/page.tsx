import Link from 'next/link'
import { GitPullRequest, ArrowUpRight } from 'lucide-react'
import { DashboardShell, SectionHeader, StatusBadge } from '@/components/dashboard-shell'

const submissions = [{ id: 'wallet-391', bounty: 'Fix wallet connection issue', repo: 'quai-community/wallet-kit', pr: '#391', status: 'VERIFYING', reward: 400, updated: 'Today, 10:42 AM' }, { id: 'sdk-218', bounty: 'Improve SDK documentation', repo: 'open-source/sdk', pr: '#218', status: 'PR_SUBMITTED', reward: 150, updated: 'Yesterday' }, { id: 'api-182', bounty: 'Fix API timeout', repo: 'quai-labs/api', pr: '#182', status: 'PAID', reward: 250, updated: 'Aug 10, 2026' }]

export default function SubmissionsPage() { return <DashboardShell title="Submissions"><SectionHeader eyebrow="Contributor workspace" title="Submissions" description="Track GitHub pull requests as they move through verification and payout."/><div className="mt-8 overflow-hidden rounded-xl border border-border bg-card"><div className="hidden grid-cols-[1.4fr_1fr_.6fr_.7fr_.8fr] gap-4 border-b border-border px-5 py-3 text-[11px] font-semibold uppercase tracking-[.1em] text-muted-foreground md:grid"><span>Bounty</span><span>Repository</span><span>PR</span><span>Status</span><span>Reward</span></div>{submissions.map(item => <Link href={`/dashboard/submissions/${item.id}`} className="grid gap-3 border-b border-border p-5 last:border-0 hover:bg-muted/50 md:grid-cols-[1.4fr_1fr_.6fr_.7fr_.8fr] md:items-center md:gap-4" key={item.id}><div><h2 className="text-sm font-semibold">{item.bounty}</h2><p className="mt-1 text-xs text-muted-foreground">Updated {item.updated}</p></div><span className="text-xs text-muted-foreground">{item.repo}</span><span className="flex items-center gap-1 text-sm font-medium"><GitPullRequest size={14}/>{item.pr}</span><span><StatusBadge status={item.status}/></span><span className="flex items-center justify-between font-mono text-sm font-semibold text-[#08784d]">{item.reward} QUAI <ArrowUpRight size={14}/></span></Link>)}</div></DashboardShell> }
