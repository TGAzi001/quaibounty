'use client'

import { useMemo, useState } from 'react'
import { Search, SlidersHorizontal } from 'lucide-react'
import { bounties } from '@/lib/mock-data'
import { BountyCard, DashboardShell, SectionHeader } from '@/components/dashboard-shell'

export default function BountiesPage() { const [query, setQuery] = useState(''); const [skill, setSkill] = useState('All'); const skills = ['All', 'React', 'TypeScript', 'Solidity', 'Go', 'Rust', 'Documentation']; const filtered = useMemo(() => bounties.filter(b => `${b.title} ${b.repo} ${b.description}`.toLowerCase().includes(query.toLowerCase())).filter(b => skill === 'All' || b.skills.includes(skill)), [query, skill]); return <DashboardShell title="Explore Bounties"><SectionHeader eyebrow="Marketplace" title="Explore bounties" description="Find funded open-source work that matches your skills."/><div className="mt-8 flex flex-col gap-3 rounded-xl border border-border bg-card p-3 sm:flex-row"><label className="flex flex-1 items-center gap-3 rounded-lg bg-muted px-3"><Search size={17} className="text-muted-foreground"/><input aria-label="Search bounties" value={query} onChange={e => setQuery(e.target.value)} placeholder="Search bounties..." className="w-full bg-transparent py-2.5 text-sm outline-none placeholder:text-muted-foreground"/></label><div className="flex items-center gap-2 overflow-x-auto"><SlidersHorizontal size={16} className="ml-1 shrink-0 text-muted-foreground"/>{skills.map(item => <button key={item} onClick={() => setSkill(item)} className={`whitespace-nowrap rounded-md px-3 py-2 text-xs font-semibold ${skill === item ? 'bg-[#071512] text-white' : 'bg-muted text-muted-foreground hover:text-foreground'}`}>{item}</button>)}</div></div><div className="mt-6 flex items-center justify-between"><p className="text-sm text-muted-foreground">{filtered.length} funded opportunities</p><button className="text-sm text-muted-foreground">Sort: Recommended</button></div><div className="mt-4 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{filtered.map(bounty => <BountyCard bounty={bounty} key={bounty.id}/>)}</div></DashboardShell> }
