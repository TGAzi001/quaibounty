import Link from 'next/link'
import { ArrowLeft, Check, Code2, ExternalLink } from 'lucide-react'
import { bounties } from '@/lib/mock-data'
import { DashboardShell, StatusBadge } from '@/components/dashboard-shell'
import { ParticipateDialog } from '@/components/participate-dialog'

export default async function BountyDetail({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const b = bounties.find(x => x.id === id) || bounties[0]; return <DashboardShell title="Bounty detail"><div className="mb-6"><Link href="/dashboard/bounties" className="flex items-center gap-2 text-sm text-muted-foreground"><ArrowLeft size={15}/>Back to bounties</Link></div><div className="flex flex-wrap items-start justify-between gap-5"><div><div className="flex items-center gap-2 text-sm text-muted-foreground"><Code2 size={16}/>{b.repo} · Issue #{b.issue}</div><div className="mt-3 flex items-center gap-3"><StatusBadge status={b.status}/><span className="text-xs text-muted-foreground">Funded escrow</span></div><h1 className="mt-3 text-3xl font-bold tracking-tight text-foreground sm:text-4xl">{b.title}</h1><p className="mt-4 max-w-2xl leading-7 text-muted-foreground">{b.description}</p></div><div className="rounded-xl border border-[#bfe4cf] bg-green-wash p-5"><div className="text-xs font-semibold text-[#08784d]">REWARD</div><div className="mt-1 font-mono text-2xl font-bold text-[#08784d]">{b.reward} QUAI</div></div></div><div className="mt-8 grid gap-5 md:grid-cols-[1.2fr_.8fr]"><section className="space-y-5"><div className="rounded-xl border border-border bg-card p-6"><div className="flex items-center justify-between"><div><div className="text-xs font-semibold uppercase tracking-[.12em] text-muted-foreground">GitHub issue</div><h2 className="mt-2 font-semibold">#{b.issue} {b.title}</h2><p className="mt-1 text-sm text-muted-foreground">{b.repo}</p></div><a href={`https://github.com/${b.repo}/issues/${b.issue}`} target="_blank" rel="noreferrer" className="flex items-center gap-1 text-xs font-semibold text-[#08784d]">Open on GitHub <ExternalLink size={13}/></a></div></div><div className="rounded-xl border border-border bg-card p-6"><h2 className="font-semibold">Description</h2><p className="mt-3 text-sm leading-7 text-muted-foreground">{b.description} This bounty is designed to reward a focused, production-ready contribution that keeps the existing API stable and easy to maintain.</p><h2 className="mt-7 font-semibold">Completion requirements</h2><p className="mt-2 text-sm text-muted-foreground">These conditions determine whether the bounty is eligible for payout.</p><div className="mt-5 space-y-3">{['Pull request must be merged', 'CI checks must pass', 'PR must target main', 'At least 1 maintainer approval', 'Tests must pass'].map(item => <div className="flex items-center gap-3 text-sm" key={item}><span className="grid size-5 place-items-center rounded-full bg-green-wash text-[#08784d]"><Check size={13}/></span>{item}</div>)}</div></div></section><aside className="h-fit rounded-xl border border-border bg-card p-6"><h2 className="font-semibold">Bounty summary</h2><div className="mt-5 space-y-4 text-sm"><div className="flex justify-between"><span className="text-muted-foreground">Reward</span><span className="font-mono font-semibold text-[#08784d]">{b.reward} QUAI</span></div><div className="flex justify-between"><span className="text-muted-foreground">Difficulty</span><span>{b.difficulty}</span></div><div className="flex justify-between"><span className="text-muted-foreground">Deadline</span><span>{b.days} days</span></div><div className="flex justify-between"><span className="text-muted-foreground">Maintainer</span><span>Quai Labs</span></div></div><div className="mt-6 border-t border-border pt-6"><ParticipateDialog bounty={b}/></div><p className="mt-4 text-center text-xs leading-5 text-muted-foreground">Reward is already funded and held in Quai escrow.</p></aside></div></DashboardShell> }
