'use client'

import { useState } from 'react'
import { Check, GitPullRequest, X } from 'lucide-react'

export function ParticipateDialog({ bounty }: { bounty: { title: string; repo: string; reward: number; days: number } }) { const [open, setOpen] = useState(false); const [accepted, setAccepted] = useState(false); return <><button onClick={() => setOpen(true)} className="flex w-full items-center justify-center gap-2 rounded-lg bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground"><GitPullRequest size={16}/>Participate</button>{open && <div className="fixed inset-0 z-50 grid place-items-center bg-[#071512]/45 p-4"><div role="dialog" aria-modal="true" aria-labelledby="participate-title" className="w-full max-w-md rounded-2xl border border-border bg-card p-6 shadow-2xl">{accepted ? <div className="text-center"><div className="mx-auto grid size-12 place-items-center rounded-full bg-green-wash text-[#08784d]"><Check size={24}/></div><h2 id="participate-title" className="mt-4 text-xl font-semibold">Bounty accepted</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">You&apos;re now participating in this bounty. Your contribution will be submitted through GitHub.</p><div className="mt-6 grid gap-2"><a href={`https://github.com/${bounty.repo}`} target="_blank" rel="noreferrer" className="rounded-lg bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground">Open GitHub issue</a><button onClick={() => setOpen(false)} className="rounded-lg border border-border px-4 py-3 text-sm font-semibold">Close</button></div></div> : <><div className="flex items-start justify-between"><div><div className="section-label">Participation</div><h2 id="participate-title" className="mt-1 text-xl font-semibold">You&apos;re about to participate</h2></div><button onClick={() => setOpen(false)} aria-label="Close dialog" className="rounded-md p-1 text-muted-foreground hover:bg-muted"><X size={18}/></button></div><div className="mt-6 rounded-xl bg-muted p-4"><h3 className="font-semibold">{bounty.title}</h3><p className="mt-1 text-xs text-muted-foreground">{bounty.repo}</p><div className="mt-4 flex justify-between text-sm"><span>Reward</span><span className="font-mono font-semibold text-[#08784d]">{bounty.reward} QUAI</span></div><div className="mt-2 flex justify-between text-sm"><span>Deadline</span><span>{bounty.days} days remaining</span></div></div><p className="mt-5 text-sm leading-6 text-muted-foreground">Your contribution will be submitted through GitHub. The bounty reward is already funded and held in escrow.</p><div className="mt-6 flex gap-3"><button onClick={() => setOpen(false)} className="flex-1 rounded-lg border border-border px-4 py-3 text-sm font-semibold">Cancel</button><button onClick={() => setAccepted(true)} className="flex-1 rounded-lg bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground">Accept bounty</button></div></>}</div></div>}</> }
